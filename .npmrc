# Configurações para pnpm
# NOTA: Este arquivo contém configurações específicas do pnpm.
# Se você usar npm, verá avisos sobre essas opções não reconhecidas.
# Recomendado: use `pnpm` conforme especificado no package.json
# Alternativa: renomeie este arquivo para .pnpmrc se quiser usar apenas npm

auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=true
node-linker=hoisted

# Configurações para CI/CD
frozen-lockfile=false
prefer-offline=true